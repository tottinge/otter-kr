#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT_DIR"

command -v uv >/dev/null 2>&1 || {
  echo "onboard: uv is required; install it from https://docs.astral.sh/uv/" >&2
  exit 1
}

uv sync
echo "onboard: environment is ready"
